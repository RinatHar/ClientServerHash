<Window x:Class="ClientHash.FormWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientHash"
        mc:Ignorable="d"
        Title="Форма для данных" Height="300" Width="500">
    <Grid x:Name="MainRoot" Background="LightGray">
        <Border MinWidth="300" Margin="15" Background="White" VerticalAlignment="Center" Padding="30" MaxWidth="500" CornerRadius="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Color="LightGray" ShadowDepth="0"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Введите данные" FontSize="26" FontWeight="Bold"/>

                <TextBox x:Name="formValue" FontSize="16"></TextBox>

                <Button FontSize="16" Content="Отправить" Margin="0 20" Height="30" Click="AddData"/>

            </StackPanel>
        </Border>
    </Grid>
</Window>
